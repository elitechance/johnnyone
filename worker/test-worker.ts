// Test entrypoint for vitest-pool-workers to enable real DO bindings with the configured miniflare options.
// Exports the DO class so the pool can register it.

export { ChatRelayDO } from './lib/runtime/chat-relay-do';